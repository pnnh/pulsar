{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pulsar-server-build",
            "command": "cmake",
            "type": "process",
            "args": [
                "--build",
                "--preset",
                "linux-clang",
                "--target",
                "pulsar-server",
                "--verbose"
            ]
        }
    ]
}